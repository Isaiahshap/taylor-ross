import React from 'react';
import Layout from '../components/Layout';
import Hero from '../components/Hero/Hero';

const Home: React.FC = () => {
  return (
    <Layout>
      <Hero />
      {/* Other sections */}
    </Layout>
  );
};

export default Home;
